from solver_CM import *
solve(row_num_M = 20, convergence=0.001,  ini_temp=6575)
#if it takes too long to run, you can increase convergence or run the following
#solve(iteration = 100000, row_num_M = 20, convergence=0.001,  ini_temp=6575)